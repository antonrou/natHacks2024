import numpy as np

def average_matrix(matrix):
    """
    Calculate the average of all elements in a matrix.

    Args:
        matrix (numpy.ndarray): A 2D NumPy array representing the matrix.

    Returns:
        float: The average of all elements in the matrix.
    """
    return np.mean(matrix)